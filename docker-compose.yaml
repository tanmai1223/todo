version: "3.8"

services:
  backend:
    build: ./todoBackend
    ports:
      - "3000:3000"
    env_file:
      - ./todoBackend/.env

  frontend:
    build: ./todo
    ports:
      - "5173:5173"
    depends_on:
      - backend